const name = "tarek";
const password = "Sony2020";
const dbName = "docu-guard";

const MONGODB_URI = `mongodb+srv://${name}:${password}@cluster0.ywzi97j.mongodb.net/${dbName}?retryWrites=true&w=majority`;

export default MONGODB_URI;
